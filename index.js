const  target = Math.floor(Math.random()*10+1); 
let guess = null;
    if guess = unfedined("Please have a guess");
    if guess = target
    if guess > prompt ('Guess lower!')
    if guess < prompt('Guess higher!')
    while guess = null
    console.log('You guessed the right number!')
